# Question 1 Explanation

- I ran `whoami` to verify the active Linux user account and observed that the session is using the `codespace` user.
- I ran `groups` to inspect the account’s group memberships and confirmed that it belongs to several development-related groups.
- I checked the shell with `echo "$SHELL"` and confirmed that the current shell is `/bin/bash`.
- I used `pwd` and `ls -la` to confirm the current working directory and list the repository contents.
- I tested network access with `curl` and observed that the environment can reach `youtube.com`, so connectivity is available.
